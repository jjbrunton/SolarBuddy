# SolarBuddy Configuration
# Copy this to .env.local and adjust values

# Database path (default: ./data/solarbuddy.db)
DB_PATH=./data/solarbuddy.db

# Server port (Next.js default: 3000)
PORT=3000

# Bind host for containerized deployments
HOSTNAME=0.0.0.0
